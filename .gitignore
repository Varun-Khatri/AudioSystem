# This repo IS a UPM package (package root). Keep .meta files committed.
#
# IMPORTANT: never add a "*~" pattern here. It matches the Samples~ folder and
# would cause git to silently ignore all package samples.
#
# Ignore only OS cruft.
.DS_Store
Thumbs.db